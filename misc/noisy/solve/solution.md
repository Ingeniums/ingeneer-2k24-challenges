use the `tr -d` command to delete the extra characters, watch out for the fact that __tr__ does take regular expressions into consideration 
add escape characters as needed, and don't forget the new line
