<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zombie Movie Hub</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            color: #222;
            margin: 0;
            padding: 20px 50px;
        }

        h1 {
            color: #222;
        }

        main {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
        }

        form {
            justify-content: start;
            width: 100%;
            margin: 20px auto;
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        label {
            color: #222;
            display: block;
            margin-bottom: 5px;
        }

        input {
            padding: 8px;
            font-size: 16px;
            width: fit-content;
        }

        button {
            padding: 8px;
            font-size: 16px;
            cursor: pointer;
            background-color: black;
            color: #fff;
            border: none;
            border-radius: 4px;
            width: fit-content;
        }

        #msg,
        #error {
            font-size: 22px;
            margin: 20px auto;
            padding: 40px;
            width: fit-content;
            background-color: white;
            border-radius: 4px;
        }

        #error {
            color: rgb(255, 87, 87);
            border: 1px solid rgb(255, 78, 78);
        }
    </style>
</head>

<body>
    <h1>Welcome to the zombie Movie Hub</h1>
    <p>Unfortunately, There are no sequels for the movies yet.</p>
    <form action="./login" method="POST">
        <label>Log in (only zombies are allowed)</label>
        <input type="text" name="username" placeholder="Enter your username">
        <input type="text" name="password" placeholder="Enter your password">
        <button type="submit">Submit</button>
    </form>

    <% if (msg) { %>
        <div id="msg">
            <%=msg%>
        </div>
        <% } %>
    
    <% if (error) { %>
        <div id="error">
            <%= error %>
        </div>
    <% } %>

</body>

</html>